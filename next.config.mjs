/** @type {import('next').NextConfig} */
const nextConfig = {
  reactCompiler: true,
  output: "export",
  basePath: "/golestan-next",
  assetPrefix: "/golestan-next/",
};

export default nextConfig;
